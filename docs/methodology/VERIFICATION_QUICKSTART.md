# Verification Quickstart

Connect to a Cardano db-sync PostgreSQL instance synced past block `13,215,210` and run the repository SQL in `../../queries/`.

Representative evidence-backed checks are documented in `../findings/MASTER_FINDINGS.md` and tied to files under `../../evidence/`.

# One-step install for the whole clone-and-ask experience.
#   py -3 -m pip install -r requirements/base.txt   (Windows)
#   python -m pip install -r requirements/base.txt  (Linux/macOS/CI)
duckdb>=1.5.0,<2
pyyaml>=6,<7
pandas>=2,<3
# This server uses the v1 FastMCP API; v2 renamed and changed that API.
mcp>=1.2.0,<2
anthropic>=0.40.0
